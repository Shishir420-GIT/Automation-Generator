# Core dependencies - Python 3.13 compatible
PyPDF2==3.0.1
streamlit>=1.28.0
google-generativeai
pymongo
tenacity

# Optional dependencies (install manually if needed)
# pandas>=2.0.0  # Only if you need data analysis features
# numpy>=1.26.0  # Only if you need numerical operations  
# plotly>=5.15.0  # Only if you need advanced plotting
# streamlit-extras>=0.3.0  # Only if you need extra UI components
